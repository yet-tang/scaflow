{
  "name": "@scaflow/state",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run tests/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@scaflow/core": "workspace:*",
    "@scaflow/schemas": "workspace:*",
    "node-sqlite3-wasm": "0.8.22"
  }
}
