{
  "name": "@scaflow/template",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "gypfile": true,
  "scripts": {
    "build": "node-gyp rebuild && tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node-gyp rebuild && vitest run tests/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "node-addon-api": "8.8.0"
  },
  "devDependencies": {
    "node-gyp": "13.0.0"
  }
}
